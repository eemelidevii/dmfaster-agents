interface:
  display_name: "DM Faster"
  short_description: "Plan and operate DM Faster campaigns safely"
  default_prompt: "Use $dmfaster to inspect my workspace, plan the campaign, preview its exact audience, and take only the actions I explicitly approve."

dependencies:
  tools:
    - type: "mcp"
      value: "dmfaster"
      description: "Official DM Faster Agent 1.0 MCP server"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
